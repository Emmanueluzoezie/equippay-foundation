{
  "name": "shared",
  "version": "1.0.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prisma:migrate": "dotenv -e ./database/.env -- prisma migrate dev --schema=./database/prisma/schema.prisma",
    "prisma:generate": "dotenv -e ./database/.env -- prisma generate --schema=./database/prisma/schema.prisma",
    "build": "tsc -b"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "Shared modules for equippay-protocol",
  "dependencies": {
    "@nestjs/core": "^10.3.10",
    "@prisma/client": "^5.17.0",
    "class-transformer": "^0.5.1",
    "class-validator": "^0.14.1",
    "init": "^0.1.2"
  },
  "devDependencies": {
    "@types/node": "^20.14.12",
    "typescript": "^5.5.4"
  }
}